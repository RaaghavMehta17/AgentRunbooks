{
  "type": "object",
  "properties": {
    "roles": {
      "type": "array",
      "items": {"type": "string"}
    },
    "budgets": {
      "type": "object",
      "properties": {
        "max_cost_per_run_usd": {"type": "number", "minimum": 0},
        "max_tokens_per_run": {"type": "integer", "minimum": 0}
      },
      "additionalProperties": false
    },
    "tool_allowlist": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": {"type": "string"}
      }
    },
    "approvals": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "step": {"type": "string"},
          "required_roles": {
            "type": "array",
            "items": {"type": "string"}
          }
        },
        "required": ["step", "required_roles"],
        "additionalProperties": false
      }
    },
    "preconditions": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "when": {"type": "string"},
          "then": {"type": "string", "enum": ["require_approval", "block", "allow"]},
          "step": {"type": "string"}
        },
        "required": ["when", "then"],
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}

